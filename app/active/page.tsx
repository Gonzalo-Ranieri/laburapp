export const dynamic = "force-dynamic" // ← stop static prerender

export default function ActivePage() {
  /* Replace the placeholder with the real Active Services UI later */
  return (
    <section className="container mx-auto py-10">
      <h1 className="mb-4 text-2xl font-semibold">Servicios activos</h1>
      <p>Aquí verás la lista de servicios que tienes en curso.</p>
    </section>
  )
}
